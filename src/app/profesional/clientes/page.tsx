'use client'
import { PageHeader } from '@/components/PageHeader'
import { useEffect, useState } from 'react'
type Client={id:string;full_name:string;phone:string|null;email:string|null;lastService:string|null;lastVisit:string}
export default function ProfessionalClients(){const [clients,setClients]=useState<Client[]>([]),[error,setError]=useState(false);useEffect(()=>{fetch('/api/professional/clients').then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(d=>setClients(d.clients??[])).catch(()=>setError(true))},[]);return <><PageHeader title="Mis clientes" description="Clientes atendidos por ti y su última visita."/>{error&&<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Conecta Supabase para cargar tus clientes.</p>}<div className="overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-[#f7f6fa] text-xs uppercase text-[#736f83]"><tr><th className="p-4">Cliente</th><th>Contacto</th><th className="hidden sm:table-cell">Último servicio</th></tr></thead><tbody>{clients.map(c=><tr key={c.id} className="border-t"><td className="p-4 font-bold">{c.full_name}</td><td>{c.phone??c.email??'—'}</td><td className="hidden sm:table-cell">{c.lastService??'—'}</td></tr>)}</tbody></table>{!error&&clients.length===0&&<p className="p-8 text-center text-sm text-[#736f83]">Aún no has atendido clientes.</p>}</div></>}
