'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
export default function ClientOnboarding(){const [error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),response=await fetch('/api/client/onboard',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({businessSlug:f.get('businessSlug'),fullName:f.get('fullName'),phone:f.get('phone')})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo completar');setLoading(false);return}router.replace('/cliente');router.refresh()}return <main className="grid min-h-[75vh] place-items-center"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border bg-white p-7"><h1 className="text-2xl font-black">Completa tu perfil</h1><p className="mt-1 text-sm text-[#736f83]">Ingresa el código del negocio donde deseas reservar.</p><label className="mt-6 block text-sm font-semibold">Código del negocio<input name="businessSlug" required placeholder="bella-studio" className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-semibold">Nombre completo<input name="fullName" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-semibold">Teléfono<input name="phone" className="mt-2 w-full rounded-xl border p-3"/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Entrar a Agen'}</button></form></main>}
