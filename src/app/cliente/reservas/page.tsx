'use client'
import { PageHeader } from '@/components/PageHeader'
import { useEffect, useState } from 'react'
type Appointment={id:string;status:string;service_period:string;professional:{display_name:string}|null;service:{name:string}|null;branch:{name:string}|null}
const rangeStart=(range:string)=>new Date(range.replace(/[\[\]()"]/g,'').split(',')[0])
export default function ClientBookings(){const [appointments,setAppointments]=useState<Appointment[]>([]),[loading,setLoading]=useState(true),[live,setLive]=useState(false),[message,setMessage]=useState('');const load=()=>fetch('/api/client/appointments').then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(d=>{setAppointments(d.appointments??[]);setLive(true)}).catch(()=>setLive(false)).finally(()=>setLoading(false));useEffect(()=>{load()},[]);async function cancel(id:string){if(!window.confirm('¿Confirmas la cancelación?'))return;const response=await fetch('/api/client/appointments',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({appointmentId:id,action:'cancel'})});const data=await response.json();setMessage(response.ok?'Reserva cancelada.':data.error??'No se pudo cancelar');if(response.ok)load()}return <><PageHeader title="Mis reservas" description="Próximas atenciones e historial."/>{message&&<p className="mb-4 rounded-xl bg-violet-50 p-3 text-sm text-violet-800">{message}</p>}{!loading&&!live&&<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Vista de demostración hasta conectar Supabase.</p>}<div className="space-y-3">{live&&appointments.map(a=>{const start=rangeStart(a.service_period);return <div key={a.id} className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><b>{a.service?.name??'Servicio'} · {start.toLocaleString('es-CL',{dateStyle:'long',timeStyle:'short'})}</b><p className="text-sm text-[#736f83]">{a.professional?.display_name} · {a.branch?.name}</p></div><div className="flex items-center gap-2"><span className="h-fit rounded-full bg-violet-50 px-3 py-1 text-xs font-bold text-violet-700">{a.status}</span>{!['CANCELLED','COMPLETED'].includes(a.status)&&<button onClick={()=>cancel(a.id)} className="rounded-lg border px-3 py-2 text-xs font-bold text-red-600">Cancelar</button>}</div></div></div>})}{live&&!loading&&appointments.length===0&&<div className="rounded-2xl border bg-white p-8 text-center text-[#736f83]">Aún no tienes reservas.</div>}</div></>}
