import { NextResponse } from 'next/server'
import { requireProfessionalContext } from '@/lib/professional-context'
import { apiError } from '@/lib/http-errors'
export const dynamic='force-dynamic'
export async function GET(request:Request){try{const {db,businessId,professional}=await requireProfessionalContext();const url=new URL(request.url),from=url.searchParams.get('from')??new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString(),until=url.searchParams.get('until')??new Date(new Date().getFullYear(),new Date().getMonth()+1,1).toISOString();const {data,error}=await db.from('appointments').select('id,quoted_price,service_period,service:services(name)').eq('business_id',businessId).eq('professional_id',professional.id).eq('status','COMPLETED').overlaps('service_period',`[${from},${until})`).order('service_period',{ascending:false});if(error)throw error;const commission=(data??[]).reduce((sum,a)=>sum+Number(a.quoted_price)*Number(professional.commission_percent)/100,0);return NextResponse.json({professional,appointments:data,commission})}catch(error){return apiError(error)}}
