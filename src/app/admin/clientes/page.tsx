'use client'
import { PageHeader } from '@/components/PageHeader'
import { NewClientModal } from '@/components/NewClientModal'
import Link from 'next/link'
import { Plus, Search } from 'lucide-react'
import { useCallback, useEffect, useState } from 'react'
type Client={id:string;full_name:string;phone:string|null;email:string|null;created_at:string;client_memory:{conversation_summary:string|null;last_interaction_at:string|null}|null}
export default function ClientsPage(){const [clients,setClients]=useState<Client[]>([]),[query,setQuery]=useState(''),[error,setError]=useState(false),[show,setShow]=useState(false);const load=useCallback(()=>{fetch(`/api/admin/clients?q=${encodeURIComponent(query)}`).then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(d=>{setClients(d.clients??[]);setError(false)}).catch(()=>setError(true))},[query]);useEffect(()=>{const timer=setTimeout(load,250);return()=>clearTimeout(timer)},[load]);return <>{show&&<NewClientModal onClose={()=>setShow(false)} onCreated={load}/>}<PageHeader title="Clientes" description="Historial, preferencias, consentimientos y valor de cada cliente." action={<button onClick={()=>setShow(true)} className="inline-flex items-center gap-2 rounded-xl bg-[#5b3df5] px-4 py-2.5 text-sm font-bold text-white"><Plus size={17}/>Nuevo cliente</button>}/>{error&&<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">Conecta Supabase para administrar clientes.</p>}<div className="mb-4 flex max-w-md items-center gap-2 rounded-xl border bg-white px-4 py-2.5"><Search size={18} className="text-[#736f83]"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nombre o teléfono" className="w-full outline-none"/></div><div className="overflow-hidden rounded-2xl border border-black/5 bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-[#f7f6fa] text-xs uppercase text-[#736f83]"><tr><th className="p-4">Cliente</th><th className="hidden sm:table-cell">Teléfono</th><th>Correo</th><th className="hidden md:table-cell">Registrado</th></tr></thead><tbody>{clients.map(c=><tr className="border-t border-black/5" key={c.id}><td className="p-4"><Link href={`/admin/clientes/${c.id}`} className="block font-bold text-[#5b3df5] hover:underline">{c.full_name}</Link><small className="text-[#736f83] sm:hidden">{c.phone}</small></td><td className="hidden sm:table-cell">{c.phone??'—'}</td><td>{c.email??'—'}</td><td className="hidden md:table-cell text-[#736f83]">{new Date(c.created_at).toLocaleDateString('es-CL')}</td></tr>)}</tbody></table>{!error&&clients.length===0&&<p className="p-8 text-center text-sm text-[#736f83]">No hay clientes para mostrar.</p>}</div></>}
