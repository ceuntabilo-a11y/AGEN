'use client'
import { PageHeader } from '@/components/PageHeader'
import { FormEvent, useEffect, useState } from 'react'
type Business={name:string;timezone:string;currency:string;phone:string|null;email:string|null;address:string|null;settings:Record<string,any>;agent_settings:Record<string,any>}
export default function SettingsPage(){const [business,setBusiness]=useState<Business|null>(null),[error,setError]=useState(''),[saved,setSaved]=useState(false);useEffect(()=>{fetch('/api/admin/settings').then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(d=>setBusiness(d.business)).catch(()=>setError('Conecta Supabase para guardar la configuración.'))},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!business)return;setSaved(false);const f=new FormData(e.currentTarget),settings={booking_interval_minutes:Number(f.get('bookingInterval')),cancellation_hours:Number(f.get('cancellationHours')),deposit_percent:Number(f.get('depositPercent')),allow_client_reschedule:f.get('allowReschedule')==='on',allow_client_cancel:f.get('allowCancel')==='on'},agent_settings={...business.agent_settings,enabled:f.get('agentEnabled')==='on',tone:f.get('agentTone')};const response=await fetch('/api/admin/settings',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),email:f.get('email'),address:f.get('address'),timezone:f.get('timezone'),currency:f.get('currency'),settings,agent_settings})});if(response.ok)setSaved(true);else setError('No se pudo guardar.')}return <><PageHeader title="Configuración" description="Identidad, políticas, moneda y agente IA."/>{error&&<p className="mb-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-800">{error}</p>}{business&&<form onSubmit={submit} className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5"><h2 className="font-extrabold">Identidad</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Nombre<input name="name" defaultValue={business.name} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Teléfono<input name="phone" defaultValue={business.phone??''} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Correo<input name="email" type="email" defaultValue={business.email??''} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Dirección<input name="address" defaultValue={business.address??''} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Zona horaria<input name="timezone" defaultValue={business.timezone} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Moneda<input name="currency" defaultValue={business.currency} className="mt-2 w-full rounded-xl border p-3"/></label></div></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-extrabold">Políticas de reserva</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Intervalo (min)<input name="bookingInterval" type="number" min="5" defaultValue={business.settings.booking_interval_minutes??15} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Cancelación previa (h)<input name="cancellationHours" type="number" min="0" defaultValue={business.settings.cancellation_hours??24} className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Anticipo (%)<input name="depositPercent" type="number" min="0" max="100" defaultValue={business.settings.deposit_percent??0} className="mt-2 w-full rounded-xl border p-3"/></label></div><label className="mt-4 flex gap-2 text-sm"><input name="allowReschedule" type="checkbox" defaultChecked={business.settings.allow_client_reschedule}/>Permitir reagendar al cliente</label><label className="mt-2 flex gap-2 text-sm"><input name="allowCancel" type="checkbox" defaultChecked={business.settings.allow_client_cancel}/>Permitir cancelar al cliente</label></section><section className="rounded-2xl border bg-white p-5 lg:col-span-2"><h2 className="font-extrabold">Agente IA</h2><div className="mt-4 flex flex-wrap gap-5"><label className="flex gap-2 text-sm"><input name="agentEnabled" type="checkbox" defaultChecked={business.agent_settings.enabled}/>Agente habilitado</label><label className="text-sm font-semibold">Tono <select name="agentTone" defaultValue={business.agent_settings.tone??'friendly'} className="ml-2 rounded-lg border p-2"><option value="friendly">Cercano</option><option value="professional">Profesional</option><option value="brief">Breve</option></select></label></div></section><button className="rounded-xl bg-[#5b3df5] px-5 py-3 font-bold text-white lg:col-span-2">Guardar configuración</button>{saved&&<p className="text-sm font-bold text-emerald-600 lg:col-span-2">Configuración guardada.</p>}</form>}</>}
