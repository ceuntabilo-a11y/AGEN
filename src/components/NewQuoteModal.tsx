'use client'
import { FormEvent, useEffect, useState } from 'react'
import { ModalShell } from '@/components/ModalShell'
export function NewQuoteModal({onClose,onCreated}:{onClose:()=>void;onCreated:()=>void}){const [clients,setClients]=useState<any[]>([]),[services,setServices]=useState<any[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{Promise.all([fetch('/api/admin/clients').then(r=>r.json()),fetch('/api/admin/catalog').then(r=>r.json())]).then(([c,s])=>{setClients(c.clients??[]);setServices(s.services??[])})},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget),service=services.find(s=>s.id===f.get('serviceId'));const response=await fetch('/api/admin/quotes',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({clientId:f.get('clientId'),validUntil:f.get('validUntil')||null,discount:Number(f.get('discount')||0),tax:Number(f.get('tax')||0),notes:f.get('notes'),items:[{serviceId:service.id,description:service.name,quantity:Number(f.get('quantity')||1),unitPrice:Number(f.get('unitPrice')||service.price),unitCost:Number(service.material_cost||0)}]})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo crear');setLoading(false);return}onCreated();onClose()}return <ModalShell titulo="Nuevo presupuesto" onClose={onClose}><form onSubmit={submit} className="w-full max-w-xl rounded-3xl bg-white p-6"><div className="flex justify-between"><div><h2 className="text-xl font-black">Nuevo presupuesto</h2><p className="text-sm text-[#736f83]">Incluye precio, costo, descuento e impuestos.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Cliente<select name="clientId" required className="mt-2 w-full rounded-xl border p-3"><option value="">Seleccionar</option>{clients.map(c=><option key={c.id} value={c.id}>{c.full_name}</option>)}</select></label><label className="text-sm font-semibold">Servicio<select name="serviceId" required className="mt-2 w-full rounded-xl border p-3"><option value="">Seleccionar</option>{services.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="text-sm font-semibold">Cantidad<input name="quantity" type="number" min="1" defaultValue="1" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Precio personalizado<input name="unitPrice" type="number" min="0" placeholder="Usar precio del servicio" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Descuento<input name="discount" type="number" min="0" defaultValue="0" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Impuesto<input name="tax" type="number" min="0" defaultValue="0" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Válido hasta<input name="validUntil" type="date" className="mt-2 w-full rounded-xl border p-3"/></label></div><label className="mt-4 block text-sm font-semibold">Notas<textarea name="notes" rows={3} className="mt-2 w-full rounded-xl border p-3"/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Crear presupuesto'}</button></form></ModalShell>}
