'use client'
import { FormEvent, useState } from 'react'
import { X } from 'lucide-react'
import { ModalShell } from '@/components/ModalShell'
export function NewBlockModal({onClose,onCreated}:{onClose:()=>void;onCreated:()=>void}){const [error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),response=await fetch('/api/professional/blocks',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({from:new Date(String(f.get('from'))).toISOString(),until:new Date(String(f.get('until'))).toISOString(),reason:f.get('reason')})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo bloquear');setLoading(false);return}onCreated();onClose()}return <ModalShell titulo="Bloquear horario" onClose={onClose}><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-black">Bloquear horario</h2><button type="button" aria-label="Cerrar" onClick={onClose}><X/></button></div><label className="mt-5 block text-sm font-semibold">Desde<input name="from" required type="datetime-local" className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-semibold">Hasta<input name="until" required type="datetime-local" className="mt-2 w-full rounded-xl border p-3"/></label><label className="mt-4 block text-sm font-semibold">Motivo<input name="reason" className="mt-2 w-full rounded-xl border p-3" placeholder="Descanso, capacitación, permiso…"/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Bloquear horario'}</button></form></ModalShell>}
