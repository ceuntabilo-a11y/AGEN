'use client'
import { FormEvent, useState } from 'react'
import { ModalShell } from '@/components/ModalShell'
export function NewClientModal({onClose,onCreated}:{onClose:()=>void;onCreated:()=>void}){const [loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);const response=await fetch('/api/admin/clients',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({fullName:form.get('fullName'),phone:form.get('phone'),email:form.get('email'),birthday:form.get('birthday')||null,notes:form.get('notes'),marketingOptIn:form.get('marketingOptIn')==='on'})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo crear');setLoading(false);return}onCreated();onClose()}return <ModalShell titulo="Nuevo cliente" onClose={onClose}><form onSubmit={submit} className="w-full max-w-lg rounded-3xl bg-white p-6"><div className="flex justify-between"><div><h2 className="text-xl font-black">Nuevo cliente</h2><p className="text-sm text-[#736f83]">Registra únicamente datos autorizados.</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Nombre<input name="fullName" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Teléfono<input name="phone" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Correo<input name="email" type="email" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Nacimiento<input name="birthday" type="date" className="mt-2 w-full rounded-xl border p-3"/></label></div><label className="mt-4 block text-sm font-semibold">Notas<textarea name="notes" className="mt-2 w-full rounded-xl border p-3" rows={3}/></label><label className="mt-4 flex gap-2 text-sm"><input name="marketingOptIn" type="checkbox"/>El cliente autorizó comunicaciones comerciales.</label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Crear cliente'}</button></form></ModalShell>}
