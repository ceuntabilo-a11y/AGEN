'use client'
import { FormEvent, useEffect, useState } from 'react'
import { X } from 'lucide-react'
export function NewServiceModal({onClose,onCreated}:{onClose:()=>void;onCreated:()=>void}){const [catalog,setCatalog]=useState<any>({specialties:[],professionals:[]}),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/catalog').then(r=>r.json()).then(setCatalog)},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const response=await fetch('/api/admin/services',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({specialtyId:f.get('specialtyId'),name:f.get('name'),description:f.get('description'),durationMinutes:Number(f.get('duration')),bufferAfterMinutes:Number(f.get('buffer')||0),price:Number(f.get('price')||0),materialCost:Number(f.get('cost')||0),depositAmount:Number(f.get('deposit')||0),professionalIds:f.getAll('professionalIds')})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo crear');setLoading(false);return}onCreated();onClose()}return <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><form onSubmit={submit} className="max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl bg-white p-6"><div className="flex justify-between"><div><h2 className="text-xl font-black">Nuevo servicio</h2><p className="text-sm text-[#736f83]">Define precio, costo y profesionales habilitados.</p></div><button type="button" onClick={onClose}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Nombre<input name="name" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Especialidad<select name="specialtyId" required className="mt-2 w-full rounded-xl border p-3"><option value="">Seleccionar</option>{catalog.specialties.map((s:any)=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="text-sm font-semibold">Duración (min)<input name="duration" required type="number" min="5" step="5" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Tiempo posterior (min)<input name="buffer" type="number" min="0" step="5" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Precio<input name="price" type="number" min="0" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Costo de materiales<input name="cost" type="number" min="0" className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Anticipo<input name="deposit" type="number" min="0" className="mt-2 w-full rounded-xl border p-3"/></label></div><label className="mt-4 block text-sm font-semibold">Descripción<textarea name="description" rows={2} className="mt-2 w-full rounded-xl border p-3"/></label><fieldset className="mt-4"><legend className="text-sm font-semibold">Profesionales habilitados</legend><div className="mt-2 grid gap-2 sm:grid-cols-2">{catalog.professionals.map((p:any)=><label key={p.id} className="flex gap-2 rounded-lg border p-3 text-sm"><input type="checkbox" name="professionalIds" value={p.id}/>{p.display_name}</label>)}</div></fieldset>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Crear servicio'}</button></form></div>}
