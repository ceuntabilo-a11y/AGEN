'use client'
import { FormEvent, useState } from 'react'
import { X } from 'lucide-react'
export function NewCampaignModal({onClose,onCreated}:{onClose:()=>void;onCreated:()=>void}){const [loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const f=new FormData(e.currentTarget);const response=await fetch('/api/admin/campaigns',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:f.get('name'),channel:f.get('channel'),content:f.get('content'),audience:{segment:f.get('segment')},scheduledAt:f.get('scheduledAt')||null})});const data=await response.json();if(!response.ok){setError(data.error??'No se pudo crear');setLoading(false);return}onCreated();onClose()}return <div className="fixed inset-0 z-50 grid place-items-center bg-black/40 p-4"><form onSubmit={submit} className="w-full max-w-xl rounded-3xl bg-white p-6"><div className="flex justify-between"><div><h2 className="text-xl font-black">Nueva campaña</h2><p className="text-sm text-[#736f83]">Solo se enviará a clientes con consentimiento vigente.</p></div><button type="button" onClick={onClose}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Nombre<input name="name" required className="mt-2 w-full rounded-xl border p-3"/></label><label className="text-sm font-semibold">Canal<select name="channel" className="mt-2 w-full rounded-xl border p-3"><option value="WHATSAPP">WhatsApp</option><option value="EMAIL">Email</option><option value="INSTAGRAM">Instagram</option><option value="MESSENGER">Messenger</option><option value="PUSH">Notificación</option></select></label><label className="text-sm font-semibold">Segmento<select name="segment" className="mt-2 w-full rounded-xl border p-3"><option value="ALL">Todos con consentimiento</option><option value="ACTIVE">Clientes activos</option><option value="INACTIVE">Clientes inactivos</option><option value="BIRTHDAY">Cumpleaños del mes</option></select></label><label className="text-sm font-semibold">Programar<input name="scheduledAt" type="datetime-local" className="mt-2 w-full rounded-xl border p-3"/></label></div><label className="mt-4 block text-sm font-semibold">Mensaje<textarea name="content" required maxLength={1500} rows={5} className="mt-2 w-full rounded-xl border p-3"/></label>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="mt-5 w-full rounded-xl bg-[#5b3df5] py-3 font-bold text-white">{loading?'Guardando…':'Crear campaña'}</button></form></div>}
