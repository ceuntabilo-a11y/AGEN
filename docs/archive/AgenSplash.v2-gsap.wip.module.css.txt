.splash {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: grid;
  place-items: center;
  overflow: hidden;
  background:
    radial-gradient(circle at 50% 45%, rgba(0, 210, 255, 0.13), transparent 34%),
    linear-gradient(145deg, #020713 0%, #050c1d 55%, #02050d 100%);
  opacity: 0;
}

.stage {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: clamp(260px, 60vw, 760px);
}

.synetiaWrap {
  position: absolute;
  width: clamp(180px, 52vw, 340px);
  aspect-ratio: 1;
  display: grid;
  place-items: center;
  opacity: 0;
}

.synetiaLogo {
  width: 100%;
  height: 100%;
  object-fit: contain;
  filter: drop-shadow(0 0 26px rgba(24, 228, 255, 0.4));
}

.agenWrap {
  position: relative;
  width: clamp(300px, 85vw, 760px);
  aspect-ratio: 1;
  opacity: 0;
}

.agenSvg {
  width: 100%;
  height: 100%;
  overflow: visible;
  filter: drop-shadow(0 0 22px rgba(0, 213, 255, 0.35));
}

.dot {
  filter: drop-shadow(0 0 10px rgba(24, 228, 255, 0.95));
}

.wordmark {
  margin-top: 18px;
  font: 600 clamp(28px, 4.5vw, 48px)/1.1 Inter, ui-sans-serif, system-ui, sans-serif;
  letter-spacing: 0.16em;
  color: #f8fbff;
  opacity: 0;
}

@media (prefers-reduced-motion: reduce) {
  .splash { transition: opacity 300ms ease; }
}
